[package]
name = "redis-cell"
version = "0.1.0"
authors = ["Brandur <brandur@mutelight.org>"]
build = "build.rs"

[lib]
crate-type = ["dylib"]

[dependencies]
bitflags = "0.7.0"
libc = "0.2.0"
time = "0.1"

[build-dependencies]
gcc = "*"
